<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MAUI_MHike.Views.ObservationListPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Observations">
    <Grid RowDefinitions="Auto,*" Padding="16" RowSpacing="8">
        <Grid Row="0" ColumnDefinitions="*,Auto">
            <Label x:Name="Hdr" Text="Hike" FontAttributes="Bold" FontSize="18" />
            <Button Grid.Column="1" Text="+ Add" Clicked="OnAddClicked" />
        </Grid>

        <RefreshView Grid.Row="1" x:Name="RefreshContainer" Refreshing="OnRefreshing">
            <CollectionView x:Name="ObsView">
                <CollectionView.EmptyView>
                    <Grid Padding="24" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="No observations yet." />
                    </Grid>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems Mode="Reveal">
                                    <SwipeItem Text="Delete" BackgroundColor="{DynamicResource ErrorContainer}"
                             Invoked="OnDeleteSwipeInvoked" />
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Frame Margin="0,6,0,0" Padding="12" HasShadow="False" CornerRadius="8"
                     BackgroundColor="{DynamicResource SecondaryContainer}">
                                <VerticalStackLayout>
                                    <Label Text="{Binding Note}" FontAttributes="Bold" />
                                    <Label Text="{Binding Time, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                           TextColor="{DynamicResource Secondary}" />
                                    <Label Text="{Binding Comments}" />
                                </VerticalStackLayout>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnItemTapped" />
                                </Frame.GestureRecognizers>
                            </Frame>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
